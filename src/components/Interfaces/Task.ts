export interface ITask {
  id: number;        // Identificador único para a tarefa (número)
  title: string;     // Título da tarefa (texto)
  difficulty: number; // Nível de dificuldade da tarefa (número)
}
